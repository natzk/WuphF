<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/swipingFragment2">
    <fragment
        android:id="@+id/favoritesFragment"
        android:name="com.example.wuphf.favorites.FavoritesFragment"
        android:label="fragment_favorites"
        tools:layout="@layout/fragment_favorites" />
    <fragment
        android:id="@+id/swipingFragment2"
        android:name="com.example.wuphf.SwipingFragment"
        android:label="fragment_swiping"
        tools:layout="@layout/fragment_swiping" >
        <action
            android:id="@+id/action_swipingFragment2_to_favoritesFragment"
            app:destination="@id/favoritesFragment" />
    </fragment>
</navigation>